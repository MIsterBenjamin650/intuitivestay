{
  "name": "@intuitive-stay/portal-server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile portal-server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.13",
    "@hono/trpc-server": "^0.4.0",
    "@intuitive-stay/api": "workspace:*",
    "@intuitive-stay/auth": "workspace:*",
    "@intuitive-stay/db": "workspace:*",
    "@intuitive-stay/env": "workspace:*",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.12",
    "stripe": "^22.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@intuitive-stay/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.16.5",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
